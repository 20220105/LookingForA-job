1.聚合函数总和：SUM
2.express：网络通信，封装。重定向响应：redirect
3.所有的定时器都是在队列里面。
process.nextTick不属于微任务，不在队列里面。
普通clg同步，定时器异步。
4.sql是数据库语言
5.isNaN(undefined)返回值是true。
6.匿名函数：没有用变量保存的函数就叫匿名函数。
7.module:当前模块对象，require用于引入模块。
_filename表示当前模块的绝对路径和模块名称。
8.异步函数不是在主线程执行，而try-catch只能捕获
主线程，也就是同步产生的错误
9.表现出来月份减一，星期天加一。
